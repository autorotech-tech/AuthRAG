# AuthRAG Agent Plane

ADK agents for Google Intensive live outside the Keept web slice:

| Path | Description |
|------|-------------|
| [`agents/keep-it-for-me`](./keep-it-for-me) | Symlink → `../../keep-it-for-me` — Keept RAG capture/search on Agent Runtime |

Web UI (`src/bookmarksBro`) continues to use `agent-api` on staging. ADK runtime is deployed in parallel via `agents-cli deploy` in `keep-it-for-me/`.

Course workspace (separate): `../ambient-expense-agent`, `../weather-assistant`.  
Secure coding lab (separate repo): `../../../secure-agent-lab`.
